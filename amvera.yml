meta:
  environment: python
  python_version: 3.11
build:
  command: pip install -r requirements.txt
run:
  # Мы добавляем флаг --chdir backend, чтобы gunicorn заходил в нужную папку
  command: gunicorn --chdir backend core.wsgi:application --bind 0.0.0.0:80